// intervalo

let start = 2;
let end = 12;
let mensagem = "o computador é preto";

mensagem.slice(0, 5); // o comp
mensagem.slice(start, end); // o computador
mensagem.substring(start, end); // o computador
mensagem.substr(start, end); // // o computador é
