// posição
let animal = "girafa";

animal[0]; // g
animal[1]; // i
animal[2]; // r

animal.at(0); // g
animal.at(1); // i
animal.at(2); // r

animal.at(-2); // f (penúltima posição)
animal.at(-1); // a (última posição)
animal.at(0); // g
animal.at(1); // i
animal.at(2); // r
